/* let sumaNumeros = 0;
for (let i = 1; i <= 20; i++) {
    sumaNumeros += i;
}
console.log(`La suma del 1 al 20 es : ${sumaNumeros}`);

//FUNCIONES ANONIMAS
const funcionAnonima = function(){
    //todo el codigo que se ejecutara
    console.log("funcion anonima");
};

//FUNCIONES FLECHA (ARROW FUNCTION)

//si la cantidad de lineas de codigo no es mas de uno, no se necesitan {}
const funcionFlecha = ()=> console.log("funcion flecha");

funcionAnonima();
funcionFlecha(); */
/* 
function retirar() {
    let retiro = parseFloat(prompt("Ingresa la cantidad que quieres retirar"));
    if (retiro > saldo) {
        alert("Saldo insuficiente para realizar esta operacion");
    } else {
        saldo -= retiro;
        alert(`Retiro exitoso. Tu saldo actual es: ${saldo.toFixed(2)}`);
    }
};

function transferencia() {
    let numCuenta = prompt("Ingresa el No. de cuenta al que deseas hacer la trasnferencia");
    monto = parseFlot(prompt("Ingresa el monto a transferir"));

    if (monto > saldo) {
        alert("Saldo insuficiente, transferencia no realizada.")
    } else {
        saldo -= monto;
        alert(`Transferencia de ${monto.toFixed(2)} realizada a la cuenta No. ${numCuenta}.
        Tu nuevo saldo es ${saldo.toFixed(2)}`);
    }
}

 */